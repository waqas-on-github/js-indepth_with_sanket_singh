// any piece of software we want to build need to store some values temporarly
// the way its stores in RAM(random access memory)
// when we run a program its called process(running program is called process)

var age = 25;
var name = "waqas";

console.log(name);
