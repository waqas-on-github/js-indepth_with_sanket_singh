// 1.toString() // this is not possiable

(1).toString(); // boxed value

let x = 1;
x.toString(); // for instance x behaved like an object not converted to object or  mutated

console.log(typeof x);
